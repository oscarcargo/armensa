{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear TipoProceso</h1>

    {{ form(form) }}

        <div class="form-actions">
        <a href="{{ path('tipoproceso') }}" class="btn btn-mini">
            Cancelar        </a>
{% endblock %}
