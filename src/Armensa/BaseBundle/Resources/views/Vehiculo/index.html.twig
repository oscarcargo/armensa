{% extends '::base.html.twig' %}
{% block body -%}
    <div class="page-header">
    	<h1>Listado de Vehículos</h1>     <a href="{{ path('vehiculo_new') }}" class="btn btn-primary btn-mini"> <i class="icon-plus icon-white"></i>
                Crear Nuevo
            </a>
        </div>
    <table class="data-tbl-simple table table-bordered table-condensed table-striped table-hover">
        <thead>
            <tr>

            	<th>Marca</th>

            	<th>Modelo</th>

            	<th>Placa</th>
                
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('vehiculo_show', { 'id': entity.id }) }}">{{ entity.marca }}</a></td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.placa }}</td>
                <td style="text-align:center"><a href="{{ path('vehiculo_show', { 'id': entity.id }) }}" > <i class="fa fa-eye"></i> </a><a href="{{ path('vehiculo_edit', { 'id': entity.id }) }}" ><i class="fa fa-pencil"></i> </a>                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
{% block javascripts -%}

{% endblock %}
