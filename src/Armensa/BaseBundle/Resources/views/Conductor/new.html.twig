{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear Conductor</h1>

    {{ form(form) }}

        <div class="form-actions">
        <a href="{{ path('conductor') }}" class="btn btn-mini">
            Cancelar        </a>
{% endblock %}
